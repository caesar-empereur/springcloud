apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-consumer
  labels:
    app: service-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-consumer
  template:
    metadata:
      labels:
        app: service-consumer
    spec:
      containers:
      - name: service-consumer
        image: service-consumer
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        
        
---
apiVersion: v1
kind: Service
metadata:
  name: service-consumer
  namespace: default
  labels:
    app: service-consumer
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: service-consumer

