apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-provider-b
  labels:
    app: service-provider-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-provider-b
  template:
    metadata:
      labels:
        app: service-provider-b
    spec:
      containers:
      - name: service-provider-b
        image: service-provider-b
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        
        
---
apiVersion: v1
kind: Service
metadata:
  name: service-provider-b
  namespace: default
  labels:
    app: service-provider-b
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: service-provider-b

